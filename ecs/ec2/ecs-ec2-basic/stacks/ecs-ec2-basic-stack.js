const cdk = require('aws-cdk-lib');
// const sqs = require('aws-cdk-lib/aws-sqs');

class EcsEc2BasicStack extends cdk.Stack {

  constructor(scope, id, props) {
    super(scope, id, props);

    
  }
}

module.exports = { EcsEc2BasicStack }
